'use strict';

var readline = require('readline');

var rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

var gameName = 'Snap';
console.log('Starting game111111 ' + gameName);